// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { FeatureFlag } from "./FeatureFlag";
import type { Maturity } from "./Maturity";

/**
 * One flag, as the settings screen needs it.
 *
 * Built from the registry rather than written out again in TypeScript, so the
 * list on screen cannot fall behind the list the code gates on.
 */
export type FeatureDescriptor = { flag: FeatureFlag, id: string, label: string, summary: string, maturity: Maturity, enabled: boolean, };
